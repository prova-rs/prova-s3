.DS_Store
/.prova
# Generated LuaLS IDE artifacts — prova writes these when you run the self-test (or `prova init`).
# The shipped stub lives in library/s3.lua (committed); these are regenerated, not tracked.
/annotations/
/.luarc.json
